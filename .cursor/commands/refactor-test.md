**Polecenie:**
Przeanalizuj plik `<file_test>` i sprawdź, czy można zrefaktoryzować znajdujące się w nim testy.  
Zaproponuj konkretne zmiany w strukturze, nazwach, asercjach lub podejściu do testowania (np. DRY, parametrization, fixture’y, mocki itp.),  
ale **nie wprowadzaj ich jeszcze** — najpierw przedstaw mi propozycję i poczekaj na moją decyzję.

Jeśli uznasz, że zmiany wymagają modyfikacji przypadków testowych, popraw również plik `<test-case-file.md>` zgodnie z nową strukturą testów,  
ale **dopiero po mojej akceptacji**.

**Dodatkowe informacje:**

- Możesz korzystać z istniejących helperów i mocków znajdujących się w pliku `testHelper.ts`.
- Nie twórz nowych mocków ani duplikatów funkcji, jeśli odpowiedni mock lub helper już istnieje.
- pamiętaj o zasadach w `vitest-unit-testing.mdc`

**Pliki do analizy:**

- `<file_test>`
- `<test-case-file.md>`
